using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;




public class colorButtonsrc : MonoBehaviour
{
    // Start is called before the first frame update

    public Button but;
    public int index;
    public int[,] list;
   

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }
    public void Swin()
    {
        //bool i = false;
        //while (true)
        //{
        //    for (int j = 0; j < 6; j++)
        //    {
        //        if (!list_col[j].active) { but.image.color = list_col[j].color; i = true; list_col[j].active = true; }
                
        //    }
        //    if (i) { break; }

        //}
        }
    //if (!listb[index])
    //{

    //    
    //    //if (index != 5)
    //    //{

    //    //    but.image.color = new Color(list[index, 0], list[index, 1], list[index, 2]);
    //    //    listb[index] = false;
    //    //    index += 1;
    //    //    listb[index] = true;

    //    //}
    //    //else { 
    //    //    but.image.color = new Color(list[index, 0], list[index, 1], list[index, 2]);
    //    //    index = 0;
    //    //}
    //}

    //else  { index += 1; Swin(); }
    //}
    

}
